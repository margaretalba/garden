import { QuartzConfig } from "./quartz/cfg"
import * as Component from "./quartz/components"

/**
 * Quartz 4.0 Configuration
 * Reset to official defaults
 */
const config: QuartzConfig = {
  configuration: {
    pageTitle: "🪴 Quartz 4.0",
    pageTitleSuffix: "",
    enableSPA: true,
    enablePopovers: true,
    analytics: {
      provider: "plausible",
    },
    locale: "en-US",
    baseUrl: "quartz.jzhao.xyz",
    ignorePatterns: ["private", "templates", ".obsidian"],
    defaultDateType: "created",
    theme: {
      fontOrigin: "googleFonts",
      cdnCaching: true,
      typography: {
        header: "Schibsted Grotesk",
        body: "Source Sans Pro",
        code: "IBM Plex Mono",
      },
      colors: {
        lightMode: {
          light: "#faf8f8",
          lightgray: "#e5e5e5",
          gray: "#b8b8b8",
          darkgray: "#4e4e4e",
          dark: "#2b2b2b",
          secondary: "#284b63",
          tertiary: "#84a59d",
          highlight: "rgba(143, 159, 169, 0.15)",
          textHighlight: "#fff23688",
        },
        darkMode: {
          light: "#161618",
          lightgray: "#393639",
          gray: "#646464",
          darkgray: "#d4d4d4",
          dark: "#ebebec",
          secondary: "#7b97aa",
          tertiary: "#84a59d",
          highlight: "rgba(143, 159, 169, 0.15)",
          textHighlight: "#b3aa0288",
        },
      },
    },
  },
  plugins: {
    transformers: [
      Component.FrontMatter(),
      Component.CreatedModifiedDate({
        priority: ["frontmatter", "filesystem"],
      }),
      Component.SyntaxHighlighting({
        theme: {
          light: "github-light",
          dark: "github-dark",
        },
        keepBackground: false,
      }),
      Component.ObsidianFlavoredMarkdown({ parseTags: true }),
      Component.GitHubFlavoredMarkdown(),
      Component.TableOfContents(),
      Component.CrawlLinks({ markdownLinkResolution: "shortest" }),
      Component.Description(),
      Component.Latex({ renderEngine: "katex" }),
    ],
    filters: [Component.RemoveDraft()],
    emitters: [
      Component.AliasRedirects(),
      Component.ComponentResources(),
      Component.ContentPage(),
      Component.FolderPage(),
      Component.TagPage(),
      Component.ContentIndex({
        enableSiteMap: true,
        enableRSS: true,
      }),
      Component.Assets(),
      Component.NotFoundPage(),
    ],
  },
}

export default config